# docs/ is committed on purpose — GitHub Pages serves it as the site root,
# so the public URL has no /dist/ segment. Rebuild before pushing.
node_modules/
.tmp/
*.local

# /dist/ here is NOT this project's build output — it holds a redirect stub for
# the old gardening-tycoon URL, tracked by THAT repo, not this one.
# This project builds to docs/.
/dist/
